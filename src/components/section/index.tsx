import "./section.scss";
type Props = {};

const Section = (props: Props) => {
  return (
    <section className="main-section">
      <div className="contents">
        <h1>Dressed and ready to eliminate competition</h1>
        <button className="btn">Discover</button>
      </div>
    </section>
  );
};

export default Section;
